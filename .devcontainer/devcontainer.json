{
  "name": "R+Python Jupyter Env",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postCreateCommand": "micromamba install -y -n base -f environment.yml && R -e \"IRkernel::installspec(user = FALSE)\"",
  "customizations": {
    "vscode": {
      "extensions": [
        "REditorSupport.r",
        "Ikuyadeu.r",
        "ms-toolsai.jupyter",
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  }
}
